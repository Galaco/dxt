package dxt

import (
	"testing"
)

func TestNewDxt1(t *testing.T) {
	t.Skip()
	//r := image.Rect(0, 0, 512, 512)
	//
	//img := NewDxt1(r)
	//packed := make([]byte, 32)
	//
	//img.Decompress(packed)
}
